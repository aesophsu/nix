# Overlay injected in outputs/aarch64-darwin/src/stella.nix (darwin modules have no inputs).
# No-oracle package from genSpecialArgs in outputs/default.nix.

{ ... }:

{
  # Placeholder; overlay added in stella.nix via inputs.nix-openclaw.overlays.default
}
