# 软件以 Nix 为准；本文件仅记录曾用 Homebrew 的项，勿直接 `brew bundle`。
# 权威清单见：modules/base/packages.nix（系统）、home/base/core、home/darwin/gui.nix（用户）、modules/darwin/apps.nix（Homebrew 例外）。
#
# 以下仅保留因 nixpkgs 暂无而仍由 Homebrew 安装的 cask：
cask "chatgpt"
#
# 曾在 VS Code 中安装的扩展（在编辑器内从市场安装即可）：
# vscode "openai.chatgpt"
