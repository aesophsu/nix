# misc

Assets not part of Nix eval (no flake output / module).

| Subdir | Role |
|--------|------|
| **certs/** | Private PKI (ECC certs, gen-certs.sh). Referenced by `modules/base/security.nix`. |
